const prompt = require('prompt-sync')();

let num1 = Number(prompt('Qual o numero?'));
let num2 = Number(prompt('Qual o numero?'));
let soma = num1 + num2;
console.log(`a soma dos numeros sera: ${soma}`);
